{
  "name": "marcos-assistant",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist/ && tsc -p tsconfig.build.json && cp package.json package-lock.json dist/",
    "deploy": "npm run build && gcloud functions deploy marcos-assistant --source dist --runtime nodejs18 --trigger-http --allow-unauthenticated --entry-point message --project marcos-assistant-383518 --env-vars-file=.env.yaml",
    "dev": "npm run build && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.3",
    "axios": "^1.3.5",
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "install": "^0.13.0",
    "npm": "^9.6.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.2",
    "@types/express": "^4.17.17",
    "prettier": "^2.8.7"
  }
}
